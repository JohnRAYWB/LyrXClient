import React from 'react';
import styles from "@/styles/HubNavBar.module.css"
const HubNavBar = () => {
    return (
        <div className={styles.main}>
        </div>
    );
};

export default HubNavBar;